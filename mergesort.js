function merge (array1, array2) {

    var returnArray = [];
    var counter = Math.max(array1.length, array2.length);

    for (var i = 0; i < counter; i++) {
          if (!array1[i] === undefined ) {

          }
        }

    }

    // for each array in args
    // take each element from them,
        // compare the two
        // push the smaller one
        // store the larger

    return returnArray;
}